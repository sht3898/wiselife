<?xml version="1.0" encoding="UTF-8"?>

<mapper namespace="wiselife.user">

	<insert id="signUp" parameterType="com.ssafy.wiselife.model.User">
	insert into user(uid, username, profile_image, is_inst, gender, year, ages, area1, area2)
	values(#{uid}, #{username}, #{profile_image}, #{is_inst}, #{gender}, #{year}, #{ages}, #{area1}, #{area2})
	</insert>

</mapper>